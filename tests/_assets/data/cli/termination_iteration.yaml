run:
  n_live: 10
  max_iterations: 1000
  n_mcmc_steps: 5
  seed: 0

moves:
  type: random_walk
  step_size: 0.3

backend:
  type: harmonic

output:
  format: none
  working_dir: "."
  info_interval: 999

termination:
  - type: iteration
    max_iterations: 5
